{
  "private": true,
  "name": "joker",
  "description": "Home of the Joker theme for VS Code",
  "author": "Leandro Nascimento",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html --out-dir build --no-autoinstall",
    "build": "parcel build index.html --out-dir build",
    "build:gh": "parcel build index.html --out-dir docs --public-url /joker"
  },
  "devDependencies": {
    "node-sass": "^4.9.3",
    "parcel-bundler": "^1.9.7"
  }
}
